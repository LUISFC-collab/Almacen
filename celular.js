/* =====================================================================
   ALMACEN CPQ · Lo propio del CELULAR

   Acá va lo que solo tiene sentido en el teléfono: la pantalla completa,
   el panel que abre el avatar, el orden de una columna.

   Se lee DESPUÉS del motor, así que puede envolver cualquier función de
   app.js sin tocarla. Lo que se escriba acá no llega a la computadora.

   Vacío por ahora: primero se cierra la app de computadora y después se
   adapta el celular, que fue lo acordado.
   ===================================================================== */
